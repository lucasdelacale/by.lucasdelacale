---
const { eyebrow, title, copy } = Astro.props;
---
<div class="section-intro">
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  <h1>{title}</h1>
  {copy && <p class="intro-copy">{copy}</p>}
</div>
