{
  "name": "acervo-criativo",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "node scripts/clean-dist.mjs && astro dev",
    "build": "node scripts/clean-dist.mjs && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "optimize": "node scripts/optimize-image.mjs"
  },
  "dependencies": {
    "@astrojs/check": "latest",
    "astro": "latest",
    "typescript": "latest"
  }
}
